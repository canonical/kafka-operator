# In Juju 3 this will be easier to copy
get-server-properties:
  description: |
    Action to retrieve the current server.properties
get-snap-apps:
  description: |
    Action to retrieve the available commands within the kafka snap

