summary: k8s kraft-tls
environment:
  TEST_ENV: kraft-tls
  KRAFT/single: single
  KRAFT/multi: multi
execute: |
  tox run -e "integration-k8s-$TEST_ENV" -- --model testing -m 'not unstable' --kraft-mode "$KRAFT"
