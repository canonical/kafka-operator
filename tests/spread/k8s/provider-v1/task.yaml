summary: k8s provider-v1
environment:
  TEST_ENV: provider-v1
  KRAFT/single: single
  KRAFT/multi: multi
execute: |
  tox run -e "integration-k8s-$TEST_ENV" -- --model testing -m 'not unstable' --kraft-mode "$KRAFT"
