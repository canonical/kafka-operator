summary: k8s charm
environment:
  TEST_ENV: charm
  KRAFT/single: single
  KRAFT/multi: multi
execute: |
  tox run -e "integration-k8s-$TEST_ENV" -- --model testing -m 'not unstable' --kraft-mode "$KRAFT"
