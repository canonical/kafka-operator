summary: k8s password-rotation
environment:
  TEST_ENV: password-rotation
  KRAFT/single: single
  KRAFT/multi: multi
execute: |
  tox run -e "integration-k8s-$TEST_ENV" -- --model testing -m 'not unstable' --kraft-mode "$KRAFT"
