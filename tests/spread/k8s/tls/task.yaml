summary: k8s tls
environment:
  TEST_ENV: tls
  KRAFT/single: single
  KRAFT/multi: multi
execute: |
  tox run -e "integration-k8s-$TEST_ENV" -- --model testing -m 'not unstable' --kraft-mode "$KRAFT"
